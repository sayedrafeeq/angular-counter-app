{
  "version": 2,
  "framework": "angular",
  "buildCommand": "npx ng build",
  "outputDirectory": "dist/counter-app",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
